{
    "name": "root",
    "private": true,
    "devDependencies": {
        "lerna": "^3.20.2",
        "rimraf": "^3.0.2"
    },
    "scripts": {
        "test": "jest",
        "build": "rimraf ./lib && tsc",
        "release": "rimraf ./lib && npm run build && npm pub",
        "dev": "tsc --watch"
    }
}
